const path = require('path')
const Path = require('path')

console.log('filemname',path.basename(__filename))
console.log('dirname',path.dirname(__filename))
console.log('extname',path.extname(__filename))